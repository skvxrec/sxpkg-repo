#!/bin/sh
./configure \
    --prefix=/usr \
    --libdir=/usr/lib \
    --bindir=/usr/bin \
    --sbindir=/usr/sbin \
    --sysconfdir=/etc \
    --libexecdir=/usr/lib \
    --with-rootprefix=/usr \
    --with-rootlibdir=/usr/lib \
    --disable-manpages \
    --disable-static
make -j$(nproc)
make DESTDIR="$1" install
